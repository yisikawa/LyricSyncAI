# LyricSync AI 操作マニュアル

このドキュメントでは、LyricSync AI のバックエンドおよびフロントエンドのセットアップと実行方法について説明します。

## 1. 事前準備

以下のツールがシステムにインストールされている必要があります。

- **Python 3.10以上** (バックエンド用)
- **Node.js 18以上** (フロントエンド用)
- **FFmpeg** (音声・動画処理用)
  - [FFmpeg公式サイト](https://ffmpeg.org/download.html)からダウンロードし、システムのPATHに含めてください。
  - バックエンド起動時に `C:\ffmpeg\bin` などの標準的なパスにあれば自動的に検出を試みます。

---

## 2. バックエンドのセットアップと起動

バックエンドは FastAPI を使用して構築されています。

### 手順
1. **ディレクトリに移動:**
   ```powershell
   cd backend
   ```

2. **仮想環境の作成と有効化:**
   ```powershell
   python -m venv venv
   .\venv\Scripts\activate
   ```

3. **依存関係のインストール:**
   ```powershell
   pip install -r requirements.txt
   ```
   ※ `faster-whisper` や `demucs` などの重いライブラリが含まれるため、インストールには数分かかる場合があります。

4. **サーバーの起動:**
   ```powershell
   uvicorn main:app --reload --port 8001
   ```
   - サーバーは `http://localhost:8001` で起動します。
   - フロントエンドとの通信のため、ポート番号は `8001` を指定してください。

---

## 3. フロントエンドのセットアップと起動

フロントエンドは React (Vite) を使用して構築されています。

### 手順
1. **ディレクトリに移動:**
   ```powershell
   cd frontend
   ```

2. **依存関係のインストール:**
   ```powershell
   npm install
   ```

3. **開発サーバーの起動:**
   ```powershell
   npm run dev
   ```
   - `http://localhost:5150` で起動します。
   - 同一ネットワーク内の他のPC・スマートフォンからは `https://<このPCのLAN IPアドレス>:5150` でアクセスできます（HTTPS自己署名証明書はmkcertにより自動生成されます）。

### LAN経由アクセス（iPhone等）の設定

LAN上の他端末からHTTPSでアクセスするには、このPCのLAN IPアドレスを環境変数で指定する必要があります。

1. `frontend/.env.example` を `frontend/.env` にコピーします。
2. `frontend/.env` の `VITE_LAN_HOST` に、このPCの実際のLAN IPアドレス（`ipconfig` で確認できる `IPv4 アドレス`）を設定します。
   ```
   VITE_LAN_HOST=192.168.x.x
   ```
3. `frontend/.env` は `.gitignore` により追跡対象外です。IPアドレスがDHCPで変わった場合は、このファイルの値のみ更新してください（バックエンドのCORS設定はプライベートIP帯を正規表現で許可しているため変更不要です）。

`start.bat` を使う場合は、ファイル冒頭の `set LAN_IP=192.168.x.x` も同じ値に更新してください（起動時のアクセスURL案内に使われます）。

---

## 4. 基本的な使用方法

各工程は画面上部の6つのタブ（ステップナビゲーション）で切り替えて進めます。
タブを切り替えることで、必要な処理が自動的に開始される設計になっています。

1. **動画選択:**
   - 動画ファイルを選択またはドラッグ＆ドロップします。
   - アップロード完了後、プレビューが表示されます。

2. **音声分離:**
   - タブをクリックすると、AIによるボーカル抽出処理が**自動的に開始**されます。
   - 処理完了まで数秒〜数分お待ちください。
   - RVC設定が有効な場合、AIカバー音声の生成も同時に行われます。

3. **文字おこし:**
   - タブをクリックすると、抽出された音声から文字起こし処理が**自動的に開始**されます。
   - 完了後、認識されたセグメントがリスト表示されます。

4. **字幕編集:**
   - プレビュー動画を見ながら歌詞の内容・タイミングを編集できます。
   - 再生ヘッドに連動して現在のセグメントがハイライト・スクロールされます。
   - エディタ上部の再生ボタンでビデオを操作できます（スマートフォンでも利用可能）。

5. **字幕動画:**
   - タブをクリックすると、元の音声＋字幕を焼き付けた動画の生成が**自動的に開始**されます。
   - 完了後、プレビューが表示されます。「保存」は共有シート/保存ダイアログ経由で端末に保存し、「DL」はサーバーから直接ダウンロードします。
   - 「再書き出し」ボタンで字幕を修正後に再生成できます。
   - 字幕がない状態でも書き出しは成功します（音声のみ置換して出力）。

6. **AI動画:**
   - タブをクリックすると、AIカバー音声＋字幕を焼き付けた動画の生成が**自動的に開始**されます。
   - AIカバー音声が未生成の場合、このタブは無効になります。
   - 操作方法は「字幕動画」と同じです。
   - 字幕がない場合でも、AIカバー音声への置換のみで書き出しを完了します。

---

## 5. アプリの終了

`start.bat` はバックエンドを別プロセスとして起動するため、フロントエンド側で `Ctrl+C` を押しても、バックエンド（uvicorn、ポート8001）は起動したまま残ります。両方を確実に停止するには、リポジトリ直下の `stop.bat` を実行してください。

```powershell
stop.bat
```

ポート8001・5150を使用しているプロセスを検出して終了します。何も起動していない状態で実行してもエラーにはなりません。

---

## トラブルシューティング

- **`start.bat` で起動に失敗する（前回のバックエンドが残っている）:**
  - `Ctrl+C` だけでは前回のバックエンドプロセスが残っていることがあります。`stop.bat` を実行してポートを解放してから、再度 `start.bat` を実行してください。
- **FFmpegが見つからない:**
  - FFmpegが正しくインストールされ、PATHが通っているか確認してください。
  - Windowsの場合、`C:\ffmpeg\bin\ffmpeg.exe` が存在することを確認してください。
- **CORSエラー:**
  - バックエンドが `8001` 番ポート以外で動いている場合、フロントエンドからのリクエストが拒否されることがあります。
- **メモリ不足:**
  - 音源分離（Demucs）や文字起こし（Whisper）はメモリを多く消費します。実行中にPCが重くなる場合は、他のアプリを閉じてみてください。
- **動画書き出しに失敗する（カバーアート埋め込み動画）:**
  - MP4にアルバムアート（カバー画像）が埋め込まれている場合、FFmpegが自動的にその画像ストリームをエンコードしようとして失敗することがありました。現在のバージョンでは `-map 0:v:0` による明示的なストリーム指定で修正済みです。
- **字幕の焼き付けに失敗する（日本語ファイル名）:**
  - libassがWindowsで日本語を含むパスを開けないことがある問題に対処済みです。字幕は一時的なASCII名ファイル経由で処理されます。字幕焼き込みが失敗した場合でも、字幕なしで書き出しを完了します。
